CREATE TABLE child (
    id INT PRIMARY KEY,
    firstName VARCHAR(50) NOT NULL,
    middleName VARCHAR(50),
    lastName VARCHAR(50),
    dateofbirthday DATE,
    sex*,
    primaryLanguage*,
    secondaryLanguage*,
    street,
    city/town,
    state*
    zipcode,
    schoolname/programname,
    phone,
    street,
    city/town,
    state*
    zipcode,

);
